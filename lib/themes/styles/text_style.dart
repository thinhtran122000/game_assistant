import 'package:flutter/material.dart';

TextStyle get mediumStyle => const TextStyle(
      fontSize: 15,
    );

TextStyle get bigStyle => const TextStyle(
      fontSize: 16,
    );

TextStyle get largeStyle => const TextStyle(
      fontSize: 18,
    );
